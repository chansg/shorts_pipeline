# ===== Unified requirements: shorts_pipeline + i2v + GUI =====

# Core
python-dotenv
numpy
pillow>=10.0.0

# GUI (python app.py)
gradio>=5.0

# Image gen (Nano Banana 2) + Veo image-to-video — needs GEMINI_API_KEY in .env
google-genai>=1.0.0

# Voiceover — ElevenLabs only (needs ELEVENLABS_API_KEY in .env)
elevenlabs

# Captions
faster-whisper

# Tests (pytest tests/)
pytest

# Optional rewrite backend (REWRITE_BACKEND="claude" in config.py)
anthropic
# REWRITE_BACKEND="ollama" uses a local Ollama service (no pip package)

# System dependency (NOT pip): ffmpeg + ffprobe must be installed and on PATH.
#   Windows:  winget install Gyan.FFmpeg     (then restart the terminal)
